{
  "name": "hai-no-tsugite",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "generate-images": "node scripts/generate-images.mjs",
    "preview": "npx serve dist"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "dotenv": "^16.4.7",
    "marked": "^15.0.0"
  }
}
